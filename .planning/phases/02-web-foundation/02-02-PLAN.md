---
phase: 02-web-foundation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/legal/PrivacyPage.jsx
  - frontend/src/pages/legal/TermsPage.jsx
  - frontend/src/pages/legal/OfferPage.jsx
  - frontend/src/components/CookieConsentBanner.jsx
autonomous: true
requirements:
  - LEGAL-02
  - LEGAL-03
  - LEGAL-04

must_haves:
  truths:
    - "Navigating to /privacy renders a readable page with 152-FZ privacy policy text"
    - "Navigating to /terms renders user agreement content"
    - "Navigating to /public-offer renders ЮKassa offer template content"
    - "All legal pages have a footer with links to /privacy, /terms, /public-offer and a contact email"
    - "Cookie consent banner appears on first visit; localStorage.cookieConsent is set after choice"
    - "Cookie banner does not appear again after page reload once consent is given"
  artifacts:
    - path: "frontend/src/pages/legal/PrivacyPage.jsx"
      provides: "Privacy policy page (152-FZ template)"
      exports: ["default PrivacyPage"]
    - path: "frontend/src/pages/legal/TermsPage.jsx"
      provides: "User agreement page"
      exports: ["default TermsPage"]
    - path: "frontend/src/pages/legal/OfferPage.jsx"
      provides: "Public offer (оферта) page for gem purchases"
      exports: ["default OfferPage"]
    - path: "frontend/src/components/CookieConsentBanner.jsx"
      provides: "Cookie consent banner with accept-all and minimal options"
      exports: ["default CookieConsentBanner"]
  key_links:
    - from: "frontend/src/components/CookieConsentBanner.jsx"
      to: "frontend/src/stores/cookieConsentStore.js"
      via: "useCookieConsentStore setCookieConsent"
      pattern: "useCookieConsentStore"
    - from: "frontend/src/pages/legal/PrivacyPage.jsx"
      to: "frontend/src/pages/legal/TermsPage.jsx"
      via: "LegalLayout shared footer links"
      pattern: "Link.*terms|Link.*privacy|Link.*public-offer"
---

<objective>
Create the three legal pages (/privacy, /terms, /public-offer) with Russian template content and the shared cookie consent banner component. These are fully self-contained: they don't depend on the routing scaffold from Plan 02-01 being wired — they are just React components that Plan 02-03 will plug into the route tree.

Purpose: Legal compliance (152-FZ) must be in place before public launch. Pages contain placeholder реквизиты marked with TODO. Cookie banner satisfies the Sep 2025 separate-consent requirement.

Output:
- 3 legal page components with template text and shared LegalLayout
- CookieConsentBanner component (custom ~60 lines, no library needed — full retro style control)
- Inline LegalLayout sub-component with footer links (reused by all three pages)
</objective>

<execution_context>
@C:/Users/Purple/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Purple/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-web-foundation/02-CONTEXT.md
@.planning/phases/02-web-foundation/02-RESEARCH.md

<interfaces>
<!-- Stores that CookieConsentBanner reads/writes -->

From frontend/src/stores/cookieConsentStore.js (created in Plan 02-01):
```javascript
// State shape:
{
  cookieConsent: null | 'accepted' | 'minimal',
  setCookieConsent: (choice: 'accepted' | 'minimal') => void
}
// Persisted to localStorage key: 'cookie-consent'
```

<!-- Navigation component for internal links in legal pages -->
From react-router-dom (available after Plan 02-01 installs deps):
- import { Link } from 'react-router-dom'  — use for internal navigation
- import { useNavigate } from 'react-router-dom'  — use for programmatic nav

<!-- Retro aesthetic rules (from CONTEXT.md decisions) -->
- Press Start 2P: only H1/H2 headings and numbers
- font-mono: body text paragraphs
- Palette: #daa520 (gold) accents, #000000 background, white text
- NES.css classes (.nes-container, .nes-btn) allowed for accent elements
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LegalLayout + three legal page components</name>
  <files>
    frontend/src/pages/legal/PrivacyPage.jsx,
    frontend/src/pages/legal/TermsPage.jsx,
    frontend/src/pages/legal/OfferPage.jsx
  </files>
  <action>
Create `frontend/src/pages/legal/` directory. Build a shared inline `LegalLayout` component used by all three pages (not a separate file — defined in a shared helper or inlined per page). Since all three are small, define a `LegalLayout` inline helper at the top of each file, or extract to `frontend/src/pages/legal/LegalLayout.jsx` and import in each page.

**Recommended approach — create `frontend/src/pages/legal/LegalLayout.jsx`:**
```jsx
import { Link } from 'react-router-dom';

const CONTACT_EMAIL = 'support@gameyourlife.ru'; // TODO: replace before prod launch

export default function LegalLayout({ title, children }) {
  return (
    <div className="min-h-screen bg-black text-white font-mono">
      {/* Header */}
      <header className="border-b border-yellow-400/20 px-6 py-4 flex items-center justify-between">
        <Link
          to="/"
          className="text-yellow-400 hover:text-yellow-300 transition-colors text-xs"
          style={{ fontFamily: "'Press Start 2P', monospace" }}
        >
          ← GAME YOUR LIFE
        </Link>
        <nav className="hidden md:flex gap-6 text-xs text-white/40">
          <Link to="/privacy" className="hover:text-white/80 transition-colors">Конфиденциальность</Link>
          <Link to="/terms" className="hover:text-white/80 transition-colors">Условия</Link>
          <Link to="/public-offer" className="hover:text-white/80 transition-colors">Оферта</Link>
        </nav>
      </header>

      {/* Content */}
      <main className="max-w-3xl mx-auto px-6 py-12">
        <h1
          className="text-yellow-400 mb-8 text-xl leading-relaxed"
          style={{ fontFamily: "'Press Start 2P', monospace" }}
        >
          {title}
        </h1>
        <div className="space-y-6 text-sm text-white/80 leading-relaxed">
          {children}
        </div>
      </main>

      {/* Footer */}
      <footer className="border-t border-white/10 px-6 py-8 mt-12">
        <div className="max-w-3xl mx-auto flex flex-col md:flex-row items-center justify-between gap-4 text-xs text-white/40">
          <p>© {new Date().getFullYear()} Game Your Life</p>
          <nav className="flex gap-4 flex-wrap justify-center">
            <Link to="/privacy" className="hover:text-white/80 transition-colors">Политика конфиденциальности</Link>
            <Link to="/terms" className="hover:text-white/80 transition-colors">Пользовательское соглашение</Link>
            <Link to="/public-offer" className="hover:text-white/80 transition-colors">Оферта</Link>
          </nav>
          <a href={`mailto:${CONTACT_EMAIL}`} className="hover:text-white/80 transition-colors">
            {CONTACT_EMAIL}
          </a>
        </div>
      </footer>
    </div>
  );
}
```

---

**PrivacyPage.jsx** (`/privacy`) — Политика конфиденциальности (152-ФЗ):
```jsx
import LegalLayout from './LegalLayout';

// TODO: Заполнить реквизиты оператора перед продакшн-запуском:
// - ИНН, ОГРН, юридический адрес, ФИО оператора ПД

export default function PrivacyPage() {
  return (
    <LegalLayout title="Политика конфиденциальности">
      <p className="text-white/50 text-xs">Редакция от 01.01.2025 · Оператор ПД: [TODO: ФИО / ИП, ИНН]</p>

      <h2 className="text-yellow-400 text-base mt-8 mb-3" style={{ fontFamily: "'Press Start 2P', monospace", fontSize: '0.75rem' }}>
        1. Общие положения
      </h2>
      <p>
        Настоящая Политика конфиденциальности (далее — «Политика») определяет порядок обработки
        персональных данных пользователей сервиса Game Your Life (далее — «Сервис»), доступного
        по адресу gameyourlife.ru. Политика разработана в соответствии с Федеральным законом
        №152-ФЗ «О персональных данных».
      </p>

      <h2 className="text-yellow-400 mt-8 mb-3" style={{ fontFamily: "'Press Start 2P', monospace", fontSize: '0.75rem' }}>
        2. Оператор персональных данных
      </h2>
      <p>
        Оператор: [TODO: ФИО или наименование ИП/ООО], ИНН: [TODO], ОГРН: [TODO],
        юридический адрес: [TODO]. Контактный email: support@gameyourlife.ru.
      </p>

      <h2 className="text-yellow-400 mt-8 mb-3" style={{ fontFamily: "'Press Start 2P', monospace", fontSize: '0.75rem' }}>
        3. Какие данные мы собираем
      </h2>
      <ul className="list-disc list-inside space-y-2 ml-2">
        <li>Адрес электронной почты (при регистрации)</li>
        <li>Отображаемое имя (display_name), выбираемое пользователем</li>
        <li>Идентификатор Telegram (telegram_id) при входе через Telegram Login</li>
        <li>IP-адрес и данные браузера (User-Agent) — технически необходимые данные</li>
        <li>Игровые данные: прогресс персонажа, квесты, транзакции виртуальной валюты</li>
      </ul>

      <h2 className="text-yellow-400 mt-8 mb-3" style={{ fontFamily: "'Press Start 2P', monospace", fontSize: '0.75rem' }}>
        4. Цели обработки
      </h2>
      <ul className="list-disc list-inside space-y-2 ml-2">
        <li>Идентификация пользователя и предоставление доступа к Сервису</li>
        <li>Обеспечение работы игровых функций и персонализации</li>
        <li>Отправка технических уведомлений (подтверждение email, сброс пароля)</li>
        <li>Обеспечение безопасности аккаунта</li>
        <li>Улучшение качества Сервиса</li>
      </ul>

      <h2 className="text-yellow-400 mt-8 mb-3" style={{ fontFamily: "'Press Start 2P', monospace", fontSize: '0.75rem' }}>
        5. Правовые основания обработки
      </h2>
      <p>
        Обработка персональных данных осуществляется на основании согласия пользователя
        (ст. 6 ч. 1 п. 1 152-ФЗ), а также в целях исполнения договора (оферты),
        стороной которого является пользователь (ст. 6 ч. 1 п. 5 152-ФЗ).
      </p>

      <h2 className="text-yellow-400 mt-8 mb-3" style={{ fontFamily: "'Press Start 2P', monospace", fontSize: '0.75rem' }}>
        6. Сроки хранения данных
      </h2>
      <p>
        Данные хранятся в течение срока действия аккаунта плюс 3 года после его удаления,
        если иное не установлено законодательством РФ. Технические логи хранятся не более 90 дней.
      </p>

      <h2 className="text-yellow-400 mt-8 mb-3" style={{ fontFamily: "'Press Start 2P', monospace", fontSize: '0.75rem' }}>
        7. Передача данных третьим лицам
      </h2>
      <p>
        Данные не передаются третьим лицам в коммерческих целях. Данные могут передаваться
        техническим подрядчикам (хостинг-провайдер, платёжная система ЮKassa) в объёме,
        необходимом для оказания услуг. Передача осуществляется только на серверы, расположенные
        на территории РФ, в соответствии со ст. 18 152-ФЗ.
      </p>

      <h2 className="text-yellow-400 mt-8 mb-3" style={{ fontFamily: "'Press Start 2P', monospace", fontSize: '0.75rem' }}>
        8. Права субъекта персональных данных
      </h2>
      <p>Вы вправе:</p>
      <ul className="list-disc list-inside space-y-2 ml-2">
        <li>Получить сведения об обработке ваших данных</li>
        <li>Потребовать исправления неточных данных</li>
        <li>Отозвать согласие и потребовать удаления данных</li>
        <li>Обратиться с жалобой в Роскомнадзор</li>
      </ul>
      <p className="mt-3">Для реализации прав обратитесь на: support@gameyourlife.ru</p>

      <h2 className="text-yellow-400 mt-8 mb-3" style={{ fontFamily: "'Press Start 2P', monospace", fontSize: '0.75rem' }}>
        9. Файлы cookie
      </h2>
      <p>
        Сервис использует технически необходимые cookie (аутентификация) и аналитические cookie
        (при вашем согласии). Управление cookie осуществляется через баннер при первом посещении
        сайта. Отказ от аналитических cookie не влияет на работу основных функций Сервиса.
      </p>

      <h2 className="text-yellow-400 mt-8 mb-3" style={{ fontFamily: "'Press Start 2P', monospace", fontSize: '0.75rem' }}>
        10. Изменения политики
      </h2>
      <p>
        Оператор вправе вносить изменения в Политику. При существенных изменениях пользователи
        будут уведомлены по email. Актуальная версия всегда доступна по адресу gameyourlife.ru/privacy.
      </p>
    </LegalLayout>
  );
}
```

---

**TermsPage.jsx** (`/terms`) — Пользовательское соглашение:
```jsx
import LegalLayout from './LegalLayout';

// TODO: Заполнить реквизиты оператора перед продакшн-запуском

export default function TermsPage() {
  return (
    <LegalLayout title="Пользовательское соглашение">
      <p className="text-white/50 text-xs">Редакция от 01.01.2025 · Версия 1.0</p>

      <h2 className="text-yellow-400 mt-8 mb-3" style={{ fontFamily: "'Press Start 2P', monospace", fontSize: '0.75rem' }}>
        1. Предмет соглашения
      </h2>
      <p>
        Настоящее Пользовательское соглашение (далее — «Соглашение») регулирует отношения
        между [TODO: ФИО/наименование оператора] (далее — «Оператор») и пользователем
        веб-сервиса Game Your Life (далее — «Сервис»). Использование Сервиса означает
        принятие условий настоящего Соглашения в полном объёме.
      </p>

      <h2 className="text-yellow-400 mt-8 mb-3" style={{ fontFamily: "'Press Start 2P', monospace", fontSize: '0.75rem' }}>
        2. Регистрация и аккаунт
      </h2>
      <ul className="list-disc list-inside space-y-2 ml-2">
        <li>Для использования Сервиса необходима регистрация (email + пароль или Telegram аккаунт)</li>
        <li>Пользователь обязуется указывать достоверные данные</li>
        <li>Пользователь несёт ответственность за сохранность учётных данных</li>
        <li>Минимальный возраст для использования Сервиса — 18 лет</li>
      </ul>

      <h2 className="text-yellow-400 mt-8 mb-3" style={{ fontFamily: "'Press Start 2P', monospace", fontSize: '0.75rem' }}>
        3. Виртуальная валюта
      </h2>
      <p>
        В Сервисе используются виртуальные игровые единицы — золото (gold) и кристаллы (gems).
        Виртуальная валюта не является денежным средством, не имеет эквивалента в реальных деньгах
        и не подлежит обмену на реальные деньги. Gold начисляется за игровые достижения бесплатно.
        Gems приобретаются за реальные деньги через платёжную систему ЮKassa и используются
        для покупок внутри Сервиса.
      </p>

      <h2 className="text-yellow-400 mt-8 mb-3" style={{ fontFamily: "'Press Start 2P', monospace", fontSize: '0.75rem' }}>
        4. Правила поведения
      </h2>
      <p>Пользователю запрещено:</p>
      <ul className="list-disc list-inside space-y-2 ml-2">
        <li>Использовать автоматизированные средства (боты, скрипты) для взаимодействия с Сервисом</li>
        <li>Проводить атаки на инфраструктуру Сервиса</li>
        <li>Создавать несколько аккаунтов для получения преимуществ</li>
        <li>Распространять заведомо ложную информацию через функции Сервиса</li>
        <li>Нарушать законодательство РФ при использовании Сервиса</li>
      </ul>

      <h2 className="text-yellow-400 mt-8 mb-3" style={{ fontFamily: "'Press Start 2P', monospace", fontSize: '0.75rem' }}>
        5. Ограничение ответственности
      </h2>
      <p>
        Сервис предоставляется «как есть» (as is). Оператор не гарантирует бесперебойную работу
        и не несёт ответственности за упущенную выгоду, потерю игрового прогресса в результате
        технических сбоев или сбоев третьих лиц. Максимальная ответственность Оператора
        ограничена суммой платежей, совершённых пользователем за последние 12 месяцев.
      </p>

      <h2 className="text-yellow-400 mt-8 mb-3" style={{ fontFamily: "'Press Start 2P', monospace", fontSize: '0.75rem' }}>
        6. Приостановление и удаление аккаунта
      </h2>
      <p>
        Оператор вправе приостановить или удалить аккаунт при нарушении настоящего Соглашения
        без возврата средств, потраченных на gems. Пользователь вправе удалить аккаунт
        самостоятельно через настройки — данные будут удалены в соответствии с Политикой
        конфиденциальности.
      </p>

      <h2 className="text-yellow-400 mt-8 mb-3" style={{ fontFamily: "'Press Start 2P', monospace", fontSize: '0.75rem' }}>
        7. Применимое право
      </h2>
      <p>
        Настоящее Соглашение регулируется законодательством Российской Федерации. Споры
        разрешаются в судебном порядке по месту нахождения Оператора, если соглашением сторон
        не установлено иное.
      </p>

      <h2 className="text-yellow-400 mt-8 mb-3" style={{ fontFamily: "'Press Start 2P', monospace", fontSize: '0.75rem' }}>
        8. Контакты
      </h2>
      <p>По вопросам, связанным с Соглашением: support@gameyourlife.ru</p>
    </LegalLayout>
  );
}
```

---

**OfferPage.jsx** (`/public-offer`) — Публичная оферта:
```jsx
import LegalLayout from './LegalLayout';

// TODO: Заполнить перед продакшн-запуском: ИНН, ОГРН, банковские реквизиты,
// shop ID ЮKassa, актуальные цены на пакеты gems

export default function OfferPage() {
  return (
    <LegalLayout title="Публичная оферта">
      <p className="text-white/50 text-xs">
        Редакция от 01.01.2025 · Оферент: [TODO: ФИО/наименование, ИНН, ОГРН]
      </p>

      <h2 className="text-yellow-400 mt-8 mb-3" style={{ fontFamily: "'Press Start 2P', monospace", fontSize: '0.75rem' }}>
        1. Предмет оферты
      </h2>
      <p>
        Настоящая публичная оферта (далее — «Оферта») является официальным предложением
        [TODO: ФИО/наименование оператора] (далее — «Оферент») заключить договор купли-продажи
        цифрового товара — пакетов кристаллов (gems) — внутри игрового сервиса Game Your Life.
        Принятием (акцептом) оферты является факт оплаты выбранного пакета.
      </p>

      <h2 className="text-yellow-400 mt-8 mb-3" style={{ fontFamily: "'Press Start 2P', monospace", fontSize: '0.75rem' }}>
        2. Описание товара
      </h2>
      <p>
        Кристаллы (gems) — цифровой игровой товар внутри Сервиса Game Your Life.
        Gems не являются средством платежа, электронными деньгами или криптовалютой.
        Gems используются исключительно для приобретения внутриигровых предметов в каталоге Сервиса.
      </p>
      <div className="mt-4 grid grid-cols-1 md:grid-cols-3 gap-4">
        {[
          { amount: '100 кристаллов', price: '[TODO] ₽', id: 'gems_100' },
          { amount: '500 кристаллов', price: '[TODO] ₽', id: 'gems_500' },
          { amount: '1500 кристаллов', price: '[TODO] ₽', id: 'gems_1500' },
        ].map((pack) => (
          <div key={pack.id} className="border border-yellow-400/30 p-4 text-center">
            <p className="text-yellow-400 font-bold text-sm">{pack.amount}</p>
            <p className="text-white/60 text-xs mt-1">{pack.price}</p>
          </div>
        ))}
      </div>

      <h2 className="text-yellow-400 mt-8 mb-3" style={{ fontFamily: "'Press Start 2P', monospace", fontSize: '0.75rem' }}>
        3. Порядок оплаты
      </h2>
      <p>
        Оплата осуществляется через платёжную систему ЮKassa (ООО «ЮMoney», ИНН 7750005725).
        После успешного платежа gems автоматически зачисляются на аккаунт пользователя.
        Доступные способы оплаты: банковская карта Visa/Mastercard/МИР, ЮMoney, СБП.
      </p>

      <h2 className="text-yellow-400 mt-8 mb-3" style={{ fontFamily: "'Press Start 2P', monospace", fontSize: '0.75rem' }}>
        4. Политика возврата
      </h2>
      <p>
        Кристаллы (gems) являются цифровым товаром. В соответствии с п. 4 ст. 26.1
        Закона РФ «О защите прав потребителей» и ст. 25 того же закона, надлежащего
        качества цифровые товары возврату и обмену не подлежат после их предоставления.
        Возврат средств возможен только в случае технической ошибки при зачислении —
        обратитесь на support@gameyourlife.ru в течение 14 дней.
      </p>

      <h2 className="text-yellow-400 mt-8 mb-3" style={{ fontFamily: "'Press Start 2P', monospace", fontSize: '0.75rem' }}>
        5. Срок действия
      </h2>
      <p>
        Gems не имеют срока действия и сохраняются на аккаунте бессрочно при соблюдении
        пользователем настоящего Соглашения. В случае удаления аккаунта пользователем
        неиспользованные gems аннулируются без компенсации.
      </p>

      <h2 className="text-yellow-400 mt-8 mb-3" style={{ fontFamily: "'Press Start 2P', monospace", fontSize: '0.75rem' }}>
        6. Контакты оферента
      </h2>
      <p>Email: support@gameyourlife.ru · [TODO: юридический адрес]</p>
    </LegalLayout>
  );
}
```
  </action>
  <verify>
    <automated>ls /c/Users/Purple/Desktop/GameUrLifeApp/GameYourLife-TWA/frontend/src/pages/legal/ && echo "Directory exists" && node -e "console.log('JSX syntax check skipped - will validate via vite build in Plan 02-03')"</automated>
  </verify>
  <done>
    - frontend/src/pages/legal/ directory exists
    - PrivacyPage.jsx, TermsPage.jsx, OfferPage.jsx, LegalLayout.jsx all exist
    - Each page exports a default React component
    - Content is in Russian with 152-FZ structure
    - TODO markers present for реквизиты
    - Footer present on LegalLayout with links to all three legal pages and email
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CookieConsentBanner component</name>
  <files>
    frontend/src/components/CookieConsentBanner.jsx
  </files>
  <action>
Custom component instead of react-cookie-consent library — avoids extra dep and gives full retro styling control. Uses cookieConsentStore (created in Plan 02-01). Resolves Pitfall 4 (z-index and safe-area-inset) from RESEARCH.md.

Create `frontend/src/components/CookieConsentBanner.jsx`:
```jsx
import { useEffect } from 'react';
import useCookieConsentStore from '../stores/cookieConsentStore';

/**
 * Cookie consent banner (152-ФЗ / GDPR).
 * Shows on first visit. Persists choice in localStorage via Zustand.
 * - Does NOT block content (position fixed, bottom of screen)
 * - z-40 so it appears above content but below modals (z-50+)
 * - Bottom padding accounts for mobile safe area + Navigation z-50
 * On mobile: banner appears ABOVE Navigation (which is z-50).
 * Banner z-40 is below Navigation — by design: user taps a tab, nav remains.
 * On desktop (≥1024px): Navigation is hidden; banner sits freely at bottom.
 */
export default function CookieConsentBanner() {
  const { cookieConsent, setCookieConsent } = useCookieConsentStore();

  // Do not render if consent already given
  if (cookieConsent !== null) return null;

  const handleAcceptAll = () => setCookieConsent('accepted');
  const handleMinimal   = () => setCookieConsent('minimal');

  return (
    <div
      role="dialog"
      aria-label="Согласие на использование cookie"
      className="fixed bottom-0 left-0 right-0 z-40 p-4"
      style={{
        // Leave room for mobile safe-area and bottom Navigation bar (~80px)
        paddingBottom: 'calc(env(safe-area-inset-bottom, 0px) + 5rem)',
      }}
    >
      {/* On desktop (lg:) no Navigation, so reduce padding */}
      <style>{`
        @media (min-width: 1024px) {
          [data-cookie-banner] { padding-bottom: 1.5rem !important; }
        }
      `}</style>

      <div
        data-cookie-banner
        className="max-w-2xl mx-auto bg-black border border-yellow-400/40 p-4 flex flex-col sm:flex-row items-start sm:items-center gap-4"
        style={{ backdropFilter: 'blur(8px)' }}
      >
        {/* Text */}
        <p className="flex-1 text-xs text-white/70 font-mono leading-relaxed">
          Мы используем cookie для аутентификации и аналитики.{' '}
          <a href="/privacy" className="text-yellow-400 hover:underline">
            Политика конфиденциальности
          </a>
        </p>

        {/* Buttons */}
        <div className="flex gap-2 shrink-0">
          <button
            onClick={handleMinimal}
            className="px-3 py-2 text-[10px] font-mono border border-white/20 text-white/50 hover:border-white/60 hover:text-white/80 transition-colors"
          >
            ТОЛЬКО НУЖНЫЕ
          </button>
          <button
            onClick={handleAcceptAll}
            className="px-3 py-2 text-[10px] font-mono bg-yellow-400 text-black hover:bg-yellow-300 transition-colors font-bold"
          >
            ПРИНЯТЬ ВСЁ
          </button>
        </div>
      </div>
    </div>
  );
}
```

Then add CookieConsentBanner to `frontend/src/main.jsx` — open the file and add the import + render it alongside App inside BrowserRouter:
```jsx
// In main.jsx, after existing imports, add:
import CookieConsentBanner from './components/CookieConsentBanner';

// Change render to:
createRoot(document.getElementById('root')).render(
  <StrictMode>
    <QueryClientProvider client={queryClient}>
      <BrowserRouter>
        <App />
        <CookieConsentBanner />
      </BrowserRouter>
    </QueryClientProvider>
  </StrictMode>
);
```

Note: This edit requires reading main.jsx first (it was written in Plan 02-01 Task 2). If Plan 02-01 ran first, main.jsx already has the QueryClientProvider + BrowserRouter structure — just add the CookieConsentBanner import and render it. If Plan 02-01 is running in parallel, coordinate the merge at the end: CookieConsentBanner must be inside BrowserRouter (it uses no router hooks currently, but future versions might).
  </action>
  <verify>
    <automated>ls /c/Users/Purple/Desktop/GameUrLifeApp/GameYourLife-TWA/frontend/src/components/CookieConsentBanner.jsx && echo "Banner file exists"</automated>
  </verify>
  <done>
    - CookieConsentBanner.jsx exists with correct default export
    - Component reads cookieConsent from useCookieConsentStore
    - Returns null when consent already set
    - Has two buttons: "ТОЛЬКО НУЖНЫЕ" (minimal) and "ПРИНЯТЬ ВСЁ" (accepted)
    - main.jsx imports and renders CookieConsentBanner inside BrowserRouter
    - z-40 positioning with safe-area-inset padding
  </done>
</task>

</tasks>

<verification>
Manual verification after both tasks complete (requires Plan 02-01 also complete for dev server):

1. Start dev server: `npm run dev`
2. Visit http://localhost:5173/privacy — renders privacy page with 152-FZ structure, footer with legal links
3. Visit http://localhost:5173/terms — renders user agreement, footer with legal links
4. Visit http://localhost:5173/public-offer — renders offer page with 3 gem pack table and TODO placeholders
5. Clear localStorage (DevTools → Application → Clear site data), reload — cookie banner appears at bottom
6. Click "ПРИНЯТЬ ВСЕ" — banner disappears; DevTools Application → localStorage → "cookie-consent" key = `{"state":{"cookieConsent":"accepted"}}`
7. Reload page — banner does NOT reappear
8. Clear localStorage again; visit /privacy — banner appears here too (it's in main.jsx, shown on all routes)
9. Click "ТОЛЬКО НУЖНЫЕ" — banner disappears; localStorage cookieConsent = "minimal"
10. On mobile viewport (375px): banner appears above the bottom tabs (not overlapping them due to padding-bottom)
</verification>

<success_criteria>
- Three legal pages exist with Russian content and 152-FZ structure
- LegalLayout has footer with links to all three legal pages + contact email
- CookieConsentBanner shows on first visit (cookieConsent === null)
- Banner disappears and localStorage is written after either button
- Banner does not show after reload when consent is stored
- Build (`npm run build`) includes these components without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-web-foundation/02-02-SUMMARY.md` with:
- Files created
- Content structure decisions (custom banner vs library rationale)
- TODO items remaining for production launch
- Verification results
</output>
