---
phase: 10.1-verify-leaderboard-and-fix-stale-rank
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/routers/quests.py
  - backend/app/crud.py
  - backend/tests/test_quests_router.py
  - .planning/phases/07-leaderboard/07-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - LEAD-01
  - LEAD-02

must_haves:
  truths:
    - "POST /api/quests/complete/{id} updates Redis ZSET leaderboard:global immediately after db.commit()"
    - "A Redis outage does not break quest completion (try/except guard — quest still completes, leaderboard update is skipped with a warning)"
    - "Unit test asserts leaderboard.update is called with correct user after a successful complete_quest call"
    - "No orphaned leaderboard.update call remains in crud.add_reward (dead code cleaned)"
    - "07-VERIFICATION.md exists at .planning/phases/07-leaderboard/ with status: passed, LEAD-01 and LEAD-02 marked SATISFIED"
    - "REQUIREMENTS.md traceability: LEAD-01 and LEAD-02 show Phase 10.1 (verify+fix), Status Verified, checkboxes [x]"
  artifacts:
    - path: "backend/app/routers/quests.py"
      provides: "complete_quest endpoint with redis_client DI and leaderboard.update call"
      contains: "Depends(cache.get_redis)"
    - path: "backend/tests/test_quests_router.py"
      provides: "Unit test asserting leaderboard.update called after successful quest completion"
      exports: ["test_complete_quest_updates_leaderboard"]
    - path: ".planning/phases/07-leaderboard/07-VERIFICATION.md"
      provides: "Goal-backward verification report for Phase 7"
      contains: "status: passed"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability for LEAD-01 and LEAD-02"
      contains: "Phase 10.1 (verify+fix)"
  key_links:
    - from: "backend/app/routers/quests.py::complete_quest"
      to: "backend/app/leaderboard.py::update"
      via: "await leaderboard.update(redis_client, user) after db.commit()"
      pattern: "leaderboard\\.update"
    - from: "backend/tests/test_quests_router.py"
      to: "backend/app/routers/quests.py::complete_quest"
      via: "direct async call with StubRedis + StubDB"
      pattern: "test_complete_quest_updates_leaderboard"
---

<objective>
Fix functional bug INT-01 (stale leaderboard ranks after quest completion) and retroactively verify Phase 7 Leaderboard by goal-backward methodology.

Purpose: Close milestone v1.0 audit gaps G1 (Phase 7 unverified, no 07-VERIFICATION.md) and G2 (complete_quest never calls leaderboard.update — Redis ZSET drifts from DB state until server restart). After this plan, LEAD-01 and LEAD-02 are fully satisfied and formally verified.

Output:
- Fixed routers/quests.py (redis DI + leaderboard.update call + try/except guard)
- Cleaned crud.py (orphaned leaderboard call removed from add_reward)
- New unit test: test_complete_quest_updates_leaderboard (103rd backend test)
- 07-VERIFICATION.md (goal-backward report — LEAD-01 SATISFIED, LEAD-02 SATISFIED)
- REQUIREMENTS.md updated (LEAD-01/02 → [x], Phase 10.1 (verify+fix), Verified)
</objective>

<execution_context>
@C:/Users/Purple/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Purple/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

@.planning/phases/10.1-verify-leaderboard-and-fix-stale-rank/10.1-RESEARCH.md
@.planning/phases/07-leaderboard/07-01-SUMMARY.md
@.planning/phases/07-leaderboard/07-02-SUMMARY.md
@.planning/phases/07-leaderboard/07-03-SUMMARY.md
@.planning/phases/07-leaderboard/07-04-SUMMARY.md

<interfaces>
<!-- Key signatures needed by executor. Extracted from Phase 7 research. -->

From backend/app/leaderboard.py (lines 23-30):
```python
async def update(redis_client, user: models.User) -> None:
    """Update user's position in leaderboard after XP/level change. Graceful: logs, never raises."""
    try:
        score = score_for(user)
        await redis_client.zadd(LEADERBOARD_KEY, {str(user.id): score})
        logger.debug(f"Leaderboard updated: user={user.id}, score={score:.2e}")
    except Exception as e:
        logger.warning(f"Leaderboard ZADD failed for user {user.id}: {e}")
```

From backend/app/cache.py (DI pattern, already used in routers/daily.py and routers/leaderboard.py):
```python
from app import cache
from redis.asyncio import Redis as aioredis

redis_client: aioredis.Redis = Depends(cache.get_redis)
```

From backend/app/crud.py (lines 73-78 — graceful guard pattern to mirror):
```python
try:
    from app import cache as _cache, leaderboard as _leaderboard
    if _cache._redis_client is not None:
        await _leaderboard.update(_cache._redis_client, user)
except Exception as e:
    logger.warning(f"Leaderboard update failed: {e}")
```

From backend/tests/conftest.py — StubRedis sorted set ops available for test:
```python
class StubRedis:
    def __init__(self): self._zsets = {}
    async def zadd(self, key, mapping): ...
    async def zrevrange(self, key, start, end, withscores=False): ...
    async def zrevrank(self, key, member): ...
    async def zcard(self, key): ...
    async def exists(self, key): ...
```

Test pattern (asyncio.run — project convention since Phase 6):
```python
def test_name():
    async def _run():
        # setup + call + assert
    asyncio.run(_run())
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire leaderboard.update into complete_quest + clean crud.add_reward orphan</name>
  <files>backend/app/routers/quests.py, backend/app/crud.py, backend/tests/test_quests_router.py</files>
  <behavior>
    - test_complete_quest_updates_leaderboard: given a successful complete_quest call with StubRedis, StubRedis._zsets["leaderboard:global"] contains an entry for user.id after the call
    - test_complete_quest_updates_leaderboard_redis_failure: if StubRedis.zadd raises Exception, complete_quest still returns success (200-equivalent), not an error
  </behavior>
  <action>
**Step 1 — Write failing tests in backend/tests/test_quests_router.py.**

The file may already exist (07-02 plan created it for router stubs). Append the new test cases.

Test 1 (`test_complete_quest_updates_leaderboard`):
- Build minimal stubs: StubRedis (from conftest), StubUser (id=42, lvl=2, xp=500, display_name="Hero"), StubDB (async no-op commit/refresh), StubQuest (id=1, xp_reward=50, gold_reward=5, hp_penalty=0, difficulty="easy", user_id=42)
- Directly call `await complete_quest(quest_id=1, user=stub_user, db=stub_db, redis_client=stub_redis)` via `asyncio.run()`
- Assert: `"42"` in `stub_redis._zsets.get("leaderboard:global", {})` (ZADD wrote the user's entry)
- Compute expected score with `leaderboard.score_for(stub_user)` and assert value matches

Test 2 (`test_complete_quest_redis_failure_doesnt_break_completion`):
- Same setup but StubRedis.zadd raises `RuntimeError("redis down")`
- Assert: complete_quest does NOT raise; returns a response object (dict or JSONResponse)

Run `pytest backend/tests/test_quests_router.py -x` — both new tests MUST FAIL (RED) before code change.

**Step 2 — Fix routers/quests.py.**

In the `complete_quest` endpoint (lines 208-270):
1. Add import at top of routers/quests.py (if not already there): `from app import leaderboard`
2. Add `redis_client: aioredis.Redis = Depends(cache.get_redis)` to the endpoint signature. Copy the import/DI pattern from routers/daily.py or routers/leaderboard.py.
3. After `await db.commit()` and `await db.refresh(user)`, add the leaderboard update block:
   ```python
   try:
       await leaderboard.update(redis_client, user)
   except Exception as e:  # noqa: BLE001
       logger.warning(f"Leaderboard update failed for user {user.id}: {e}")
   ```
   Place AFTER the XP/lvl mutation, AFTER db.commit(), AFTER db.refresh(user) — so user object has current lvl/xp when score is computed.

**Step 3 — Clean crud.py orphan.**

In `backend/app/crud.py::add_reward` (lines 52-80):
- Remove the leaderboard.update call block (the try/except that calls `_leaderboard.update`). Do NOT delete the entire `add_reward` function — just remove the leaderboard block inside it. The function itself is dead code from the legacy Telegram path; leave the rest as-is (do not delete the function body) to avoid disrupting any import-time effects.
- Add a one-line comment above `add_reward`: `# Legacy Telegram-id path — unreachable from JWT routers. Leaderboard update moved to routers/quests.py::complete_quest (Phase 10.1).`

**Step 4 — Verify GREEN.**

Run `pytest backend/tests/test_quests_router.py -x` — both new tests must PASS.
Run `pytest backend/tests/ -x` — full suite must pass (target: 103 tests).
  </action>
  <verify>
    <automated>pytest backend/tests/test_quests_router.py::test_complete_quest_updates_leaderboard pytest backend/tests/test_quests_router.py::test_complete_quest_redis_failure_doesnt_break_completion -xvs</automated>
    <automated>pytest backend/tests/ -x</automated>
  </verify>
  <done>
    - routers/quests.py complete_quest signature includes redis_client: aioredis.Redis = Depends(cache.get_redis)
    - leaderboard.update called after db.commit() with try/except guard
    - crud.add_reward no longer calls leaderboard.update; comment explains legacy status
    - test_complete_quest_updates_leaderboard passes (ZADD assertion)
    - test_complete_quest_redis_failure_doesnt_break_completion passes (no raise)
    - Full pytest suite: 103/103 pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 07-VERIFICATION.md and sync REQUIREMENTS.md traceability</name>
  <files>.planning/phases/07-leaderboard/07-VERIFICATION.md, .planning/REQUIREMENTS.md</files>
  <action>
**Step 1 — Create .planning/phases/07-leaderboard/07-VERIFICATION.md.**

Follow the exact format of .planning/phases/05.1-verify-web-foundation/05.1-VERIFICATION.md (frontmatter + sections). This is a retroactive goal-backward verification artifact for Phase 7.

Frontmatter:
```yaml
---
phase: 07-leaderboard
verified: 2026-04-28T00:00:00Z
status: passed
score: 6/6 must-haves verified
gaps: []
human_verification:
  - item: "Plan 07-04 browser smoke test"
    status: deferred
    reason: "User directive 2026-04-27: 'Давай просто дальше по плану'"
    moved_to: "Pre-Launch Checklist (see below)"
re_verification: false
gap_closure: true
closes: [G1, G2]
audit: v1.0-MILESTONE-AUDIT.md (2026-04-28)
---
```

Body sections to include:

**Phase Goal:** Глобальный лидерборд по уровню/XP с позицией пользователя.

**Goal Achievement — Observable Truths** (table format matching 05.1-VERIFICATION.md):
List 6 truths from roadmap Phase 10.1 success criteria, each row: # | Truth | Status | Evidence
Truths:
1. Redis sorted set `leaderboard:global` updates when user completes a quest → ✓ VERIFIED → `routers/quests.py::complete_quest` calls `leaderboard.update(redis_client, user)` after `db.commit()` (wired in Phase 10.1); `test_complete_quest_updates_leaderboard` asserts ZADD call
2. `GET /api/leaderboard?offset=0&limit=50` returns ranked top entries → ✓ VERIFIED → `routers/leaderboard.py` GET / endpoint; `leaderboard.get_top()` calls `zrevrange` then hydrates display_name from DB; 07-01, 07-02 SUMMARYs confirm
3. `GET /api/leaderboard/me` returns user's rank ±5 neighbors → ✓ VERIFIED → `routers/leaderboard.py` GET /me endpoint; `leaderboard.get_me()` with bounds-safe neighbor window; test_get_me_rank passes
4. LeaderboardPage at `/app/leaderboard` shows display_name, lvl, xp with user's row highlighted → ✓ VERIFIED → `frontend/src/pages/LeaderboardPage.jsx` real API binding; gold highlight on `entry.rank === userRank`; 07-03 SUMMARY confirms
5. Score formula `lvl*1e12 + xp*1e6 - id` encodes correct rank ordering in single float → ✓ VERIFIED → `leaderboard.score_for()` in leaderboard.py; test_score_ordering in test_leaderboard.py asserts descending by lvl then xp then id
6. Unit test suite for leaderboard domain passes (14 tests in test_leaderboard.py) → ✓ VERIFIED → `pytest backend/tests/test_leaderboard.py` 14/14 pass

**Required Artifacts** (table):
- `backend/app/leaderboard.py` — domain module (score_for, update, get_top, get_me, seed_if_empty) → ✓ VERIFIED
- `backend/app/routers/leaderboard.py` — GET /api/leaderboard, GET /api/leaderboard/me → ✓ VERIFIED
- `backend/app/schemas.py` — LeaderboardEntryResponse, LeaderboardResponse, LeaderboardMeResponse → ✓ VERIFIED
- `backend/tests/test_leaderboard.py` — 14 unit tests for domain logic → ✓ VERIFIED
- `backend/tests/test_quests_router.py::test_complete_quest_updates_leaderboard` — ZADD assertion for G2 fix → ✓ VERIFIED (added Phase 10.1)
- `frontend/src/pages/LeaderboardPage.jsx` — real data binding + gold highlight → ✓ VERIFIED

**Requirements Coverage** (table: Requirement | Verdict | Evidence):
- LEAD-01 | ✓ SATISFIED | Redis ZSET populated at startup (seed_if_empty) and kept current by leaderboard.update call in complete_quest (Phase 10.1 fix); GET /api/leaderboard returns ranked list; LeaderboardPage renders it
- LEAD-02 | ✓ SATISFIED | GET /api/leaderboard/me returns user's authoritative rank from Redis; LeaderboardPage highlights row where entry.rank === userRank (server-authoritative comparison, not stale prop)

**Key Link Verification** (table):
- complete_quest → leaderboard.update | await leaderboard.update(redis_client, user) after db.commit() | ✓ WIRED (Phase 10.1 fix; test asserts ZADD call)
- leaderboard.update → Redis ZADD | zadd(LEADERBOARD_KEY, {str(user.id): score}) | ✓ WIRED (leaderboard.py lines 23-30)
- LeaderboardPage → /api/leaderboard | leaderboardService fetch in useEffect | ✓ WIRED (07-03 SUMMARY)
- LeaderboardPage → /api/leaderboard/me | leaderboardService fetch for user rank | ✓ WIRED (07-03 SUMMARY)

**Deferred Verification** section:
Document Plan 07-04 deferred status explicitly:
- Plan 07-04 (browser human-verify checkpoint) was DEFERRED at user direction 2026-04-27 ("Давай просто дальше по плану" — recorded in 07-04-SUMMARY.md)
- Browser smoke test items moved to Pre-Launch Checklist (do NOT repeat those items here — they are already in 07-04-SUMMARY.md)
- Phase 7 is VERIFIED by: code artifacts + 14+1 unit tests + build pass. Browser end-to-end smoke test is non-blocking documentation check for pre-launch.

**Gap Closure** section:
- G1 (Phase 7 unverified) — CLOSED: this document is the goal-backward verification artifact
- G2 (leaderboard.update not wired in complete_quest) — CLOSED: fix in routers/quests.py (Phase 10.1), unit test asserts ZADD
- G3 (AUTH-05 stub) — CARRIED FORWARD: no change from prior audit; Phase 11/PROD-03 delivers SMTP

**Pre-Launch Checklist** section (non-blocking, must complete before v1.0 public launch):
- [ ] Browser smoke test from 07-04-SUMMARY.md Pre-Launch Checklist (items 1-5): startup log, LeaderboardPage render, quest-complete → rank refresh, hard refresh loading state, error state + retry button

**Step 2 — Update .planning/REQUIREMENTS.md.**

Find the two LEAD rows in the Traceability table (around lines 176-177):
```
| LEAD-01 | Phase 7 → Phase 10.1 (verify+fix) | Pending ... |
| LEAD-02 | Phase 7 → Phase 10.1 (verify+fix) | Pending ... |
```

Change both rows to:
```
| LEAD-01 | Phase 10.1 (verify+fix) | Verified |
| LEAD-02 | Phase 10.1 (verify+fix) | Verified |
```

Also update the `[ ]` checkboxes for LEAD-01 and LEAD-02 in the requirements list section (lines ~44-45) to `[x]`. Remove the inline note text "(partial (G2 stale-rank bug...); fix in Phase 10.1)" and replace with a clean description matching verified requirements:
```
- [x] **LEAD-01**: User can view a global leaderboard of players ranked by level and XP
- [x] **LEAD-02**: User's own rank and position are highlighted on the leaderboard
```

Update the Coverage summary near the bottom: move LEAD-01 and LEAD-02 from "Partial" to "Verified" count (29 → 31 satisfied, 4 partial → 2 partial).
  </action>
  <verify>
    <automated>python -c "import pathlib; v=pathlib.Path('.planning/phases/07-leaderboard/07-VERIFICATION.md').read_text(); assert 'status: passed' in v; assert 'LEAD-01' in v; assert 'LEAD-02' in v; assert 'SATISFIED' in v; print('07-VERIFICATION.md OK')"</automated>
    <automated>python -c "import pathlib; r=pathlib.Path('.planning/REQUIREMENTS.md').read_text(); assert '[x] **LEAD-01**' in r; assert '[x] **LEAD-02**' in r; assert 'Phase 10.1 (verify+fix)' in r; assert '| Verified |' in r; print('REQUIREMENTS.md OK')"</automated>
  </verify>
  <done>
    - .planning/phases/07-leaderboard/07-VERIFICATION.md exists with frontmatter status: passed
    - 07-VERIFICATION.md covers both LEAD-01 and LEAD-02 with SATISFIED verdicts and concrete evidence
    - Deferred Plan 07-04 status documented explicitly (not re-opened; just recorded)
    - G1 and G2 closure documented in the verification report
    - REQUIREMENTS.md LEAD-01 and LEAD-02 checkboxes changed to [x]
    - REQUIREMENTS.md traceability rows updated: Phase 10.1 (verify+fix) | Verified
    - Coverage summary count updated (31 satisfied, 2 partial)
  </done>
</task>

</tasks>

<verification>
Run from backend/ directory:

```bash
pytest backend/tests/ -x
```

Expected: 103 tests pass (102 existing + 1 new test_complete_quest_updates_leaderboard).

Spot checks:
- `grep -n "leaderboard" backend/app/routers/quests.py` — should show the import and the update call
- `grep -n "leaderboard.update" backend/app/crud.py` — should return 0 hits (orphan removed)
- `grep -n "test_complete_quest_updates_leaderboard" backend/tests/test_quests_router.py` — should show the new test
- `.planning/phases/07-leaderboard/07-VERIFICATION.md` exists with `status: passed`
- `grep "LEAD-01" .planning/REQUIREMENTS.md` shows `[x]` and `Phase 10.1 (verify+fix)` and `Verified`
</verification>

<success_criteria>
Phase 10.1 is complete when ALL of the following hold:

1. `pytest backend/tests/ -x` passes with 103 tests (regression: 0 regressions, 1 new test added)
2. `backend/app/routers/quests.py::complete_quest` signature contains `redis_client: aioredis.Redis = Depends(cache.get_redis)` and body contains `await leaderboard.update(redis_client, user)` inside a try/except block placed after `db.commit()`
3. `backend/app/crud.py::add_reward` does NOT contain a `leaderboard.update` call (orphan removed, comment added)
4. `.planning/phases/07-leaderboard/07-VERIFICATION.md` exists with `status: passed`, LEAD-01 ✓ SATISFIED, LEAD-02 ✓ SATISFIED, G1+G2 closure documented
5. `.planning/REQUIREMENTS.md` shows `[x] **LEAD-01**`, `[x] **LEAD-02**`, traceability rows `Phase 10.1 (verify+fix) | Verified`
6. Milestone v1.0 audit gaps G1 and G2 are formally closed
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-verify-leaderboard-and-fix-stale-rank/10.1-01-SUMMARY.md` following the standard summary template.
</output>
