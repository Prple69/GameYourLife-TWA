---
phase: 04-character-stats
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - backend/app/routers/quests.py
autonomous: true
requirements:
  - PROG-03

must_haves:
  truths:
    - "POST /api/quests/save persists the category field from request body"
    - "POST /api/quests/complete/{id} increases the correct stat by STAT_GROWTH[difficulty] when quest.category is not null"
    - "POST /api/quests/complete/{id} returns stat_gain object (or null for legacy category=NULL quests)"
    - "POST /api/analyze prompt includes 4 stat level lines + category + the 'weak-stat boost' rule"
    - "POST /api/analyze continues to return existing shape (difficulty/xp/gold/hp_penalty) — no breaking response change"
    - "Stat level-up loop terminates via apply_stat_xp safety counter"
  artifacts:
    - path: "backend/app/routers/quests.py"
      provides: "Extended save_quest, complete_quest, analyze_task"
      contains: "apply_stat_xp"
  key_links:
    - from: "backend/app/routers/quests.py::complete_quest"
      to: "backend/app/utils/game_logic.py::apply_stat_xp"
      via: "from app.utils.game_logic import apply_stat_xp, STAT_GROWTH, CATEGORY_TO_STAT"
      pattern: "apply_stat_xp\\(user,"
    - from: "backend/app/routers/quests.py::save_quest"
      to: "models.Quest.category column"
      via: "models.Quest(category=quest_data.category, ...)"
      pattern: "category=quest_data.category"
    - from: "backend/app/routers/quests.py::analyze_task"
      to: "user.stat_*_level values"
      via: "prompt string interpolation via authenticated user"
      pattern: "СТАТЫ ИГРОКА"
---

<objective>
Wire the backend behavior for Phase 4 into `backend/app/routers/quests.py`: (1) `save_quest` persists category, (2) `complete_quest` applies stat XP via `apply_stat_xp` and returns `stat_gain` in response, (3) `analyze_task` prompt includes the 4 stat lines + category + the "weak-stat boost" rule while keeping the JSON response contract unchanged.

Purpose: This is the contract layer for all frontend work in Plan 04-03/04-04. The frontend category picker (04-03) sends `category` in save/analyze payloads; the frontend completion popup (04-04) reads `stat_gain` from the response. Without this plan, frontend wiring has nothing to talk to.

Output: One modified file (`backend/app/routers/quests.py`). Zero schema changes — all schema work is in 04-01. Endpoint contracts extended but backward compatible on the response side (stat_gain can be null for legacy quests).
</objective>

<execution_context>
@C:/Users/Purple/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Purple/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-character-stats/04-CONTEXT.md
@.planning/phases/04-character-stats/04-RESEARCH.md
@.planning/phases/04-character-stats/04-01-SUMMARY.md

# File being modified
@backend/app/routers/quests.py

# Depended-upon contract from Plan 04-01
@backend/app/utils/game_logic.py
@backend/app/schemas.py

<interfaces>
<!-- Contracts from Plan 04-01 — available after 04-01 completes. -->

**From `backend/app/utils/game_logic.py` (Plan 04-01 artifact):**
```python
CATEGORY_TO_STAT: dict[QuestCategory, StatName] = {
    "work": "endurance", "fitness": "strength",
    "learning": "wisdom", "social": "charisma",
}
STAT_GROWTH: dict[str, int] = {"easy": 1, "medium": 2, "hard": 4, "epic": 8}
def max_xp_for_level(level: int) -> int: ...
def apply_stat_xp(user, stat_name, gain: int) -> dict:
    """Mutates user in place. Returns {'name','xp_gained','leveled_up','new_level'}."""
```

**From `backend/app/schemas.py` (Plan 04-01 artifact):**
- `QuestCategory = Literal["work", "fitness", "learning", "social"]`
- `QuestSave` now has required `category: QuestCategory`
- `UserSchema` exposes stat_* fields
- `QuestSchema` has `category: Optional[QuestCategory] = None`

**From `backend/app/models.py` (Plan 04-01 artifact):**
- `User.stat_{strength,wisdom,endurance,charisma}_{level,xp}` columns with server_default
- `Quest.category` nullable String column

**Current `complete_quest` body (must be extended, not rewritten)** — existing flow you preserve:
```python
quest.is_completed = True
user.xp += int(quest.xp_reward * user.xp_multiplier)
user.gold += int(quest.gold_reward * user.gold_multiplier)
leveled_up = False
while user.xp >= user.max_xp:
    user.xp -= user.max_xp
    user.lvl += 1
    user.max_xp = int(user.max_xp * 1.2)
    leveled_up = True
await db.commit(); await db.refresh(user)
return {"status": "success", "leveled_up": leveled_up, "user": UserSchema(...),
        "reward": {"xp": quest.xp_reward, "gold": quest.gold_reward}}
```

**Current `analyze_task` prompt skeleton** — you add a block and a rule, DO NOT rewrite:
```
СТАТУС ИГРОКА:
- Уровень: {lvl}
- Текущее HP: {current_hp} / {max_hp}

КРИТЕРИИ СЛОЖНОСТИ И НАГРАД: ...
ПРАВИЛА МАСТЕРА:
1. Если дедлайн критический ...
2. Оцени "hp_penalty" ...
3. Если у игрока критически мало HP ...
```
Insert new СТАТЫ block + new КАТЕГОРИЯ line after the STATUS block and before КРИТЕРИИ; add Rule 4 after Rule 3.

**Current `save_quest` signature** — already uses `quest_data: schemas.QuestSave`, so just pipe `quest_data.category` into the ORM constructor.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend complete_quest with stat gain + save_quest with category persistence</name>
  <files>backend/app/routers/quests.py, backend/tests/test_quests_router.py</files>
  <behavior>
    Test cases (write BEFORE implementation — use FastAPI TestClient with authenticated fixture, or direct function calls with a stub DB session):
    - `complete_quest` on a quest with `category="fitness"` and `difficulty="medium"` adds 2 stat XP to user.stat_strength_xp (fitness→strength, medium→2)
    - `complete_quest` on a quest with `category="work"` and `difficulty="hard"` adds 4 stat XP to user.stat_endurance_xp (work→endurance, hard→4); user.stat_strength_xp unchanged
    - `complete_quest` response dict includes `stat_gain={"name":"endurance","xp_gained":4,"leveled_up":False,"new_level":1}` when category is set
    - `complete_quest` on a **legacy** quest where `category IS NULL` adds 0 stat XP to all 4 stats (no-op) and response has `stat_gain = None` (or key absent) — NO KeyError raised
    - `complete_quest` still performs character-level-up (user.lvl increments when user.xp >= user.max_xp) — existing behavior preserved
    - `save_quest` called with `QuestSave(category="social", ...)` creates a Quest row with `category == "social"`
    - `save_quest` called without `category` raises Pydantic ValidationError (422 from FastAPI) — enforced by the schema from 04-01
  </behavior>
  <action>
**Part A — tests first (RED).** Create/extend `backend/tests/test_quests_router.py`. Minimum test approach: use FastAPI's `TestClient` plus an async in-memory SQLite or a test-fixture user/quest directly created via AsyncSession, OR (simpler, acceptable) unit-test the stat-application logic by instantiating a stub User and calling the internal function. The critical assertion is on the stat increment + stat_gain return value; full HTTP testing is welcomed but not required if the project lacks a test client setup.

If a test client fixture does not already exist in the repo, write a **lean** stub test: construct a `models.User()` and `models.Quest()` by hand (do not hit real DB), then call a small helper or the relevant code path. Skip tests that strictly require a running DB if they would require heavy new fixture infra — prefer unit-level tests over integration here. The goal is regression coverage, not full e2e. Run `pytest` — tests must fail initially.

**Part B — extend `save_quest` (GREEN step 1).** In `backend/app/routers/quests.py::save_quest`, the current body constructs `models.Quest(user_id=..., title=..., difficulty=..., xp_reward=..., gold_reward=..., hp_penalty=..., deadline=..., is_completed=False, is_failed=False)`. Add one kwarg: `category=quest_data.category`. Because `QuestSave` from Plan 04-01 now requires category, FastAPI will 422 any caller missing it — no manual check needed.

**Part C — extend `complete_quest` (GREEN step 2).** Add the import at the top of the file:
```python
from app.utils.game_logic import apply_stat_xp, STAT_GROWTH, CATEGORY_TO_STAT
```

After the existing character-level-up `while` loop and BEFORE `await db.commit()`, insert the stat-gain block:
```python
# Phase 4: stat gain (no-op for legacy quests without category)
stat_gain = None
if quest.category is not None:
    stat_name = CATEGORY_TO_STAT[quest.category]
    xp_gain = STAT_GROWTH[quest.difficulty]
    stat_gain = apply_stat_xp(user, stat_name, xp_gain)
```

Update the return dict to include `stat_gain`:
```python
return {
    "status": "success",
    "leveled_up": leveled_up,
    "user": schemas.UserSchema.model_validate(user),
    "reward": {"xp": quest.xp_reward, "gold": quest.gold_reward},
    "stat_gain": stat_gain,  # None for legacy category=NULL quests
}
```

**Guards (Pitfall 7 from research):** The `if quest.category is not None` check is critical. If `category` is an empty string or unknown value somehow, `STAT_GROWTH[quest.difficulty]` could still raise KeyError on unknown difficulty — that's a pre-existing concern, don't expand scope here, but log if encountered.

Run `pytest backend/tests/test_quests_router.py -v` — tests pass (GREEN).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_quests_router.py -v</automated>
  </verify>
  <done>
- `complete_quest` response JSON contains `stat_gain` key (object or null).
- `save_quest` persists `category` to DB.
- Legacy quest (`category=NULL`) completes without error and returns `stat_gain=None`.
- All test cases in `<behavior>` block pass.
- No changes to the function signatures of `save_quest` or `complete_quest` — only bodies extended.
  </done>
</task>

<task type="auto">
  <name>Task 2: Augment analyze_task prompt with stat block + weak-stat boost rule</name>
  <files>backend/app/routers/quests.py</files>
  <action>
In `analyze_task` (around line 62 of `backend/app/routers/quests.py`), the prompt is a multi-line f-string. You need two extensions, both inside the same `prompt = f"""..."""` literal:

**Extension 1 — new СТАТЫ + КАТЕГОРИЯ block.** After the existing STATUS block (ends with `- Текущее HP: {current_hp} / {max_hp}`) and BEFORE `КРИТЕРИИ СЛОЖНОСТИ И НАГРАД:`, insert:
```
        СТАТЫ ИГРОКА (level):
        - Сила: {user.stat_strength_level}
        - Мудрость: {user.stat_wisdom_level}
        - Выносливость: {user.stat_endurance_level}
        - Обаяние: {user.stat_charisma_level}

        КАТЕГОРИЯ КВЕСТА: {category}
```

The `user` variable is already available via `user: models.User = Depends(get_current_user)`. The `category` variable needs to be extracted from the payload; add one line near the top of the try-block alongside the existing `title = payload.get(...)`:
```python
category = payload.get("category", "unknown")
```
Accept "unknown" as a graceful fallback if category missing (don't raise — we don't want /analyze to break for in-flight tooling during rollout).

**Extension 2 — new rule 4.** After the existing rule 3 (ending `...но не ниже минимального для категории.`) add rule 4:
```
        4. Если стат, соответствующий категории квеста ({category}), НИЖЕ среднего
        по 4 статам — слегка увеличь XP/gold (+10%) и снизь hp_penalty (-10%),
        чтобы поощрить прокачку слабого направления. Не меняй формат ответа.
```

**DO NOT change:**
- The response JSON schema block at the end (`{{ "difficulty": ..., "xp": ..., "gold": ..., "hp_penalty": ... }}`).
- The model name or client configuration.
- The `json.loads(clean_json)` parsing.
- The fallback dict (`{"difficulty": "medium", ...}`).

The prompt grows by ~8 lines; output format is identical — the existing `json.loads` parser still works. AI hint is soft; model may or may not honor it, that's acceptable for Phase 4 (per RESEARCH.md Pitfall 4, we accept inherent variance).

**Verification** (manual smoke, not automated): after deploy, log the full prompt once at DEBUG level for the first 2-3 `/analyze` calls to confirm the block is rendered. Delete this logging in 04-04 verify checkpoint if added. For this plan's automated verify, just check prompt contents via a quick string assertion.

Add a simple string test to `backend/tests/test_quests_router.py`: import the router module, patch `AsyncOpenAI.chat.completions.create` to return a canned response, call `analyze_task` with a stub user having stat_*_level set, and assert that the prompt string passed to the client contains `"СТАТЫ ИГРОКА"`, `"Сила:"`, `"КАТЕГОРИЯ КВЕСТА:"`, `"слабого направления"`. If patching AsyncOpenAI is heavy, a simpler alternative is extracting the prompt into a helper `def _build_analyze_prompt(title, deadline, ..., user, category) -> str` and testing that helper directly — but only do this refactor if it's trivial. Otherwise, skip the prompt unit test and rely on manual smoke.
  </action>
  <verify>
    <automated>cd backend && grep -E "СТАТЫ ИГРОКА|КАТЕГОРИЯ КВЕСТА|слабого направления" app/routers/quests.py | wc -l | tr -d ' '</automated>
  </verify>
  <done>
- `analyze_task` prompt contains the 4-line СТАТЫ ИГРОКА block using `user.stat_*_level`.
- Prompt contains `КАТЕГОРИЯ КВЕСТА: {category}` line.
- Prompt contains rule 4 referencing "слабого направления" (weak stat hint).
- Response JSON schema block unchanged (difficulty/xp/gold/hp_penalty keys only).
- `category = payload.get("category", "unknown")` added so frontend payload's category is used.
- Fallback dict unchanged (`{"difficulty": "medium", ...}`).
- Grep verify finds at least 3 matches (the three required phrases).
  </done>
</task>

</tasks>

<verification>
After both tasks:

1. **Stat gain happens on completion**: Create a quest with `category="fitness"`, difficulty="hard"; complete it; assert `user.stat_strength_xp` increased by 4 in DB (and response `stat_gain.xp_gained == 4`).
2. **Legacy compat**: Complete a quest with `category=NULL` (directly set via `UPDATE quests SET category=NULL WHERE id=X` if needed); assert no 500 error, `stat_gain` is None in response.
3. **Save persists category**: POST to /api/quests/save with `category="learning"`; subsequent GET returns quest with `category="learning"`.
4. **Save rejects missing category**: POST to /api/quests/save without `category` field returns 422 (Pydantic validation error).
5. **AI prompt contains stats**: `grep "СТАТЫ ИГРОКА" backend/app/routers/quests.py` matches; manual smoke call to /api/analyze with category="social" returns valid JSON with difficulty/xp/gold/hp_penalty.
6. **Unit tests pass**: `pytest backend/tests/` green.
</verification>

<success_criteria>
1. `backend/app/routers/quests.py::save_quest` passes `category=quest_data.category` to `models.Quest(...)`.
2. `backend/app/routers/quests.py::complete_quest` imports from `game_logic` and branches on `quest.category is not None` before calling `apply_stat_xp`.
3. Response body of `/api/quests/complete/{id}` includes `stat_gain` field (object or null).
4. `analyze_task` prompt includes СТАТЫ block + КАТЕГОРИЯ line + rule 4; response shape unchanged.
5. `category = payload.get("category", "unknown")` extracts category from /analyze payload.
6. All tests in `backend/tests/test_quests_router.py` pass.
7. No breaking change to existing endpoint URLs, HTTP methods, or required fields beyond what 04-01 schema already enforces.
</success_criteria>

<output>
After completion, create `.planning/phases/04-character-stats/04-02-SUMMARY.md` recording:
- Exact lines of the 3 edits in quests.py (save_quest category, complete_quest stat-gain block, analyze_task prompt block)
- Response shape of POST /api/quests/complete/{id} (full JSON example for Plan 04-04 to consume)
- Any smoke test result (did a real /analyze call with category come back with altered reward? note if AI obeyed or ignored the hint — informational only)
</output>
