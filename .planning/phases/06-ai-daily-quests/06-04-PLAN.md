---
phase: 06-ai-daily-quests
plan: 04
type: execute
wave: 3
depends_on: [06-02, 06-03]
files_modified: []
autonomous: false
requirements: [AI-01, AI-02]

must_haves:
  truths:
    - "User sees 'Квесты дня' section on /app/quests"
    - "Clicking 'Получить квесты дня' shows 3 quest cards after a moment"
    - "Each card shows title, category chip, difficulty, xp/gold/hp_penalty"
    - "Accepting a card removes it and adds quest to active list"
    - "Rerolling replaces one card; counter updates"
    - "Second page load shows same suggestions (Redis cache working)"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete AI Daily Quests feature.

Purpose: Automated tests cover logic but cannot verify visual rendering, UX flow, or real LLM integration. This checkpoint confirms the full feature works end-to-end in the browser with a real Redis instance running.
Output: Human confirmation that Phase 6 success criteria are met.
</objective>

<execution_context>
@C:/Users/Purple/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Purple/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ai-daily-quests/06-CONTEXT.md
@.planning/phases/06-ai-daily-quests/06-02-SUMMARY.md
@.planning/phases/06-ai-daily-quests/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start Redis + backend + frontend for verification</name>
  <files></files>
  <action>
    Verify dev environment is ready:

    1. **Start Redis** (if not already running):
       ```bash
       # Option A: Docker Compose (if docker available)
       docker-compose up -d redis

       # Option B: Windows - start Redis directly if installed
       # Or use WSL: wsl redis-server --daemonize yes
       ```

    2. **Install new dependencies** (redis[asyncio] added in Plan 01):
       ```bash
       cd backend
       pip install -r requirements.txt
       ```

    3. **Verify Redis ping** from Python:
       ```bash
       cd backend
       python -c "import asyncio; import redis.asyncio as r; asyncio.run(r.Redis.from_url('redis://localhost:6379/0').ping()); print('Redis OK')"
       ```

    4. **Start backend**:
       ```bash
       cd backend
       uvicorn app.main:app --reload --port 8000
       ```
       Look for log: `Redis connected: redis://localhost:6379/0`

    5. **Start frontend** in a separate terminal:
       ```bash
       cd frontend
       npm run dev
       ```

    Run the full test suite as a final automated check before human verify:
    ```bash
    cd backend && python -m pytest tests/ -x --tb=short
    ```
    All tests must be green before proceeding to checkpoint.
  </action>
  <verify>
    <automated>cd C:/Users/Purple/Desktop/GameUrLifeApp/GameYourLife-TWA/backend && python -m pytest tests/ -x --tb=short 2>&1 | tail -10</automated>
  </verify>
  <done>All backend tests pass; backend starts without Redis crash; frontend dev server running.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human verify AI Daily Quests end-to-end</name>
  <what-built>
    Complete AI Daily Quests feature (Phase 6):
    - Backend: GET /api/daily/suggestions (LLM + Redis cache), POST /api/daily/accept/{index} (creates quest), POST /api/daily/reroll/{index} (rerolls one card, 2/day limit)
    - Frontend: "Квесты дня" section in QuestsPage with trigger button, DailyQuestCard components, accept/reroll flow
  </what-built>
  <how-to-verify>
    Open http://localhost:5173, log in, navigate to /app/quests.

    **Test 1 — Initial state:**
    1. Scroll to top of Quests page
    2. Confirm "КВЕСТЫ ДНЯ" section appears ABOVE the active quests list
    3. Confirm "ПОЛУЧИТЬ КВЕСТЫ ДНЯ" button is visible (no auto-load)

    **Test 2 — Generate suggestions:**
    4. Click "ПОЛУЧИТЬ КВЕСТЫ ДНЯ"
    5. Wait for loading ("ГЕНЕРАЦИЯ...")
    6. Confirm 3 quest cards appear, each showing: title, category chip (colored), difficulty, +XP/+GOLD/-HP values
    7. Confirm "✓ ПРИНЯТЬ" and "↻ РЕРОЛЛНУТЬ" buttons visible on each card

    **Test 3 — Reroll counter visibility:**
    8. Before any reroll: confirm counter is NOT visible (or shows 0/2 if implemented that way)
    9. Click "↻ РЕРОЛЛНУТЬ" on card 1
    10. Confirm that card's content changes (new suggestion)
    11. Confirm reroll counter updates (shows "Рероллов: 1/2" or similar)

    **Test 4 — Accept flow:**
    12. Click "✓ ПРИНЯТЬ" on one card
    13. Confirm toast "Квест добавлен!" appears briefly
    14. Confirm the accepted card DISAPPEARS from the daily section
    15. Confirm the new quest now appears in the Active Quests list below

    **Test 5 — Redis caching:**
    16. Refresh the page (F5)
    17. Click "ПОЛУЧИТЬ КВЕСТЫ ДНЯ" again
    18. Confirm the same suggestions appear (same titles), NOT a new LLM call (should be instant from cache)

    **Test 6 — Empty state:**
    19. Accept remaining cards (or exhaust rerolls until section empties)
    20. Confirm empty state shows "Завтра новые квесты" (or similar message)
    21. Confirm the daily section is still visible (does not disappear entirely)

    **Test 7 — Slot cap (if you have 5 active quests):**
    22. If active quest count is 5: confirm Accept button appears grayed out with tooltip "Освободи слот (5/5)"

    **Test 8 — Personalization check (AI-02):**
    23. Navigate to /app/character, note which stat is lowest for your character
    24. Return to /app/quests, look at the 3 suggestion categories
    25. Confirm at least one suggestion's category matches the weak stat (work=Сила/Выносливость, fitness=Выносливость, learning=Мудрость, social=Обаяние)
    26. This is probabilistic — LLM may not always honor it; just visually check.

    Report any issues with the UX flow, visual rendering, or errors in browser console.
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe specific issues found (e.g., "cards not showing", "accept button broken", "Redis not caching")</resume-signal>
  <action>Human manually tests all 8 scenarios listed in how-to-verify. No automated action — this is a verification-only checkpoint.</action>
  <verify>
    <automated>MISSING — this is a human-verify checkpoint; automated verification is in Task 1 above</automated>
  </verify>
  <done>Human types "approved" after confirming all Phase 6 success criteria in the browser.</done>
</task>

</tasks>

<verification>
All 8 manual test scenarios pass per human verification above.
</verification>

<success_criteria>
Phase 6 success criteria (from ROADMAP.md) verified:
1. GET /api/daily/suggestions returns 3 AI-generated quests ✓
2. Suggestions account for weak user stats (low Charisma → social quest) ✓
3. Result cached in Redis key daily:{user_id}:{YYYY-MM-DD} (TTL to midnight) — confirmed by same suggestions on page refresh ✓
4. User can accept (creates quest) or reroll (no more than 2 times/day) ✓
5. AI prompt includes lvl, last 10 quests, stats ✓
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-daily-quests/06-04-SUMMARY.md`
</output>
