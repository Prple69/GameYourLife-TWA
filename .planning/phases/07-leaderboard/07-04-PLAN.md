---
phase: 07-leaderboard
plan: "04"
type: execute
wave: 3
depends_on:
  - "07-02"
  - "07-03"
files_modified: []
autonomous: false
requirements:
  - LEAD-01
  - LEAD-02

must_haves:
  truths:
    - "User can navigate to /app/leaderboard and see real player data (not mock heroes)"
    - "User's own row is visually highlighted in gold in the table"
    - "The rank plate at top shows correct rank number matching the user's position in the table"
    - "Completing a quest and returning to leaderboard reflects updated rank"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the end-to-end leaderboard feature in a real browser.

Purpose: Confirms LEAD-01 and LEAD-02 observable behaviors from the user's perspective — live data, correct highlight, rank plate accuracy, and post-quest rank update.

Output: User approval (or issue report for gap closure).
</objective>

<execution_context>
@C:/Users/Purple/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Purple/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-leaderboard/07-CONTEXT.md
@.planning/phases/07-leaderboard/07-02-SUMMARY.md
@.planning/phases/07-leaderboard/07-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Human Verify: End-to-end leaderboard in browser</name>
  <action>No automated action — human verifies the completed leaderboard feature in browser per steps below.</action>
  <verify>Human confirms all checklist items pass (see how-to-verify).</verify>
  <done>User types "approved" confirming real data, highlight, rank plate, and post-quest update all work correctly.</done>
  <what-built>
    - Backend: `GET /api/leaderboard?offset=0&limit=100` and `GET /api/leaderboard/me` endpoints
    - Backend: `crud.add_reward` updates Redis ZSET after every quest completion
    - Backend: startup seeds the leaderboard from PostgreSQL if Redis is empty
    - Frontend: LeaderboardPage fetches real data; columns are rank / display_name / lvl / xp (no class column)
    - Frontend: User's row highlighted in gold (#daa520/20 bg) when rank ≤ 100
    - Frontend: Rank plate shows real rank from /api/leaderboard/me
    - Frontend: Loading state and error retry button present
  </what-built>
  <how-to-verify>
    1. Start backend: `cd backend && uvicorn app.main:app --reload`
       - Check startup logs: should see "Leaderboard already seeded: N users" or "Leaderboard seed complete: N users"

    2. Start frontend: `cd frontend && npm run dev`
       - Open http://localhost:5173 and log in

    3. Navigate to `/app/leaderboard`
       - Verify: Table shows real display names (not "Hero_1", "Hero_2" mock data)
       - Verify: Columns are `#`, `Герой`, `Ур`, `XP` (no "Класс" / class column)
       - Verify: Your username appears in the rank plate at top with a real rank number (not "#242")
       - Verify: Your row in the table is highlighted with a gold background
       - Verify: Header subtitle shows "Топ N героев" with actual player count

    4. Test quest completion → rank update:
       - Go to `/app/quests`, complete an active quest
       - Return to `/app/leaderboard`
       - Verify: Rank plate and table reflect updated state (XP column changes; rank updates on level-up)

    5. Test loading state:
       - Hard-refresh the leaderboard page
       - Verify: Brief loading indicator appears before data renders

    6. (Optional) Test error state:
       - Stop backend server, refresh leaderboard page
       - Verify: Error message + "Повторить" button appears
       - Restart backend, click "Повторить" — data loads successfully
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues found (e.g., "rank plate shows null", "class column still visible")</resume-signal>
</task>

</tasks>

<verification>
End-to-end human verification of LEAD-01 (global leaderboard with real data) and LEAD-02 (user's rank highlighted).
</verification>

<success_criteria>
- User approves the leaderboard feature in browser
- LEAD-01: Real ranked player list visible at /app/leaderboard
- LEAD-02: User's own row highlighted; rank plate shows correct position
</success_criteria>

<output>
After completion, create `.planning/phases/07-leaderboard/07-04-SUMMARY.md`
</output>
